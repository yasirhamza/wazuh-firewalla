[pytest]
testpaths = tests
# Integration tests are excluded by default (require a live OpenSearch). Run
# them explicitly with `-m integration`, or `-m ""` to include everything.
addopts = -v --tb=short -m "not integration"
markers =
    integration: tests that require a live OpenSearch (skip by default)
asyncio_mode = auto
