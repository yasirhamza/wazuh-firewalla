# Firewalla-Wazuh Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control

# ===========================================
# Wazuh Indexer Credentials
# ===========================================
# Password for the 'admin' user (OpenSearch/Indexer access)
# Generate a strong password: openssl rand -base64 32
INDEXER_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# Wazuh Dashboard Credentials
# ===========================================
# Password for 'kibanaserver' user (internal dashboard auth)
# Can usually be left as default
DASHBOARD_PASSWORD=kibanaserver

# ===========================================
# Wazuh API Credentials
# ===========================================
# Password for 'wazuh-wui' API user
# Must match the password configured in the Wazuh manager
API_PASSWORD=CHANGE_ME_API_PASSWORD

# ===========================================
# Firewalla MSP Integration (Optional)
# ===========================================
# Only needed if using the msp-poller sidecar
# Get these from your Firewalla MSP portal
MSP_DOMAIN=your-domain.firewalla.net
MSP_TOKEN=your_msp_api_token_here
